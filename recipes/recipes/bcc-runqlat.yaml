apiVersion: kai.recipe/v1
name: bcc-runqlat
version: 0.30.0
upstream:
  repo: https://github.com/iovisor/bcc.git
  ref: v0.30.0
license: GPL-2.0
build:
  commands:
    - cd tools && clang -target bpf -O2 runqlat.c -c -o runqlat.o
outputs:
  programs:
    - path: tools/runqlat.o
