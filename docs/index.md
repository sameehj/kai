# KAI Documentation

KAI is an OpenClaw-compatible AI tool gateway. Tools live in `~/.kai/tools/<name>/` and are exposed via MCP.

- Start MCP: `./bin/kai-mcp`
- Start Gateway: `./bin/kai-gateway`
- List tools: `kai tools list`

See `docs/mcp.md` for MCP examples.
