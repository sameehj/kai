logLevel: info

tools:
  paths:
    - ~/.kai/tools

gateway:
  address: 127.0.0.1:9910
  allowedAddrs: []
  maxSessions: 0

exec:
  timeout: 30s
  maxOutput: 1048576
  blocklist:
    - rm
    - dd
    - mkfs
    - shutdown
    - reboot
