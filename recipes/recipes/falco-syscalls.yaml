apiVersion: kai.recipe/v1
name: falco-syscalls
version: 0.37.0
upstream:
  repo: https://github.com/falcosecurity/falco.git
  ref: 0.37.0
license: Apache-2.0
build:
  commands:
    - make -C driver all
outputs:
  programs:
    - path: driver/falco_bpf.o
