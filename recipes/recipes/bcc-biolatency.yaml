apiVersion: kai.recipe/v1
name: bcc-biolatency
version: 0.30.0
upstream:
  repo: https://github.com/iovisor/bcc.git
  ref: v0.30.0
license: GPL-2.0
build:
  commands:
    - cd tools && clang -target bpf -O2 biolatency.c -c -o biolatency.o
outputs:
  programs:
    - path: tools/biolatency.o
